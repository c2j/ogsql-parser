//! <include> 片段解析。

use crate::ibatis::error::IbatisError;
use crate::ibatis::types::MapperFile;

pub fn resolve_includes(_mapper: &MapperFile) -> Result<MapperFile, IbatisError> {
    todo!("Task 3")
}

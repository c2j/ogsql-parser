[package]
name = "ogsql-parser"
version = "0.1.0"
edition = "2021"
description = "A SQL parser for openGauss/GaussDB written in Rust"
license = "MIT OR Apache-2.0"

[dependencies]
thiserror = "2"
encoding_rs = "0.8"

[dev-dependencies]
walkdir = "2"

所有CMS异常导致实例不可用 所有CMS异常导致实例不可用 故障现象 CMS是 GaussDB 的实例管理模块，用来管理和监控分布式系统中各个功能单元和物理资源的运行情况，确保整个系统的稳定运行。如果实例中所有CMS组件状态都为“down”，那么这个实例将不可用。 以混合部署（4副本）为例，如 下图 所有CMS组件都损坏，其他部署形态同理。 图1 CMS故障 业务影响 实例中所有CMS都损坏，实例将无法进行仲裁，如果此时其他组件全部正常，那整个实例能够正常运行，但当任意一个其他组件发生异常，因为实例无法自动切换，整个实例将不可用。 故障原因 CMS故障原因有以下几种： CMS节点机器掉电、断网等。 CMS磁盘全故障，磁盘满。 CMS配置文件被移除。 CMS端口被占用。 预期处理时长 约50分钟。 日志收集要求 参考 下载管理节点日志 和 下载租户节点日志 收集各模块日志路径。 Agent： /home/Ruby/log/asyncJobResult.dat /home/Ruby/log/agent.log GaussDB Kernel： /var/lib/log/Ruby 处理思路 CMS进程异常需要及时处理，避免所有CMS组件都异常。如果CMS已经全部损坏，需要尽快将CMS恢复。恢复完成一个CMS之后，在ETCD正常的情况下，修复的CMS会自动升主。然后就可以执行命令将整个实例重新启动，让业务恢复起来，然后再修复剩下的CMS。 故障定位 实例不可用。 参考 登录实例节点 ，登录CMS所在节点，并进入沙箱。 执行如下命令，导入环境变量。 source /home/Ruby/gauss_env_file 然后执行如下指令查询集群状态。 cm_ctl query -Cv id 发现CMS组件全部异常。 前提条件 以下修复操作需要在沙箱执行，进入沙箱的操作指导如下： 执行如下指令，进入沙箱环境。 /usr/sbin/chroot  /var/chroot 请忽略 bash: EulerOS_history: command not found 的提示。 执行如下指令，设置环境变量。 /bin/env LD_LIBRARY_PATH="/usr/local/app/lib:/usr/local/app/add-ons" >>/dev/null && source /etc/profile && source ~/.bashrc 请忽略 bash: EulerOS_history: command not found 的提示。 故障恢复 参考 登录实例节点 ，登录CMS所在节点，并进入沙箱。 执行如下命令，导入环境变量。 source /home/Ruby/gauss_env_file 然后执行如下指令查询集群状态。 cm_ctl query -Cv 图2 集群状态 如图所示，CMS的“state”全部为“Down”，可以排除被执行命令停止的情况。 如果CMS所在的节点都已经损坏，请登录任意一个可以登录的节点。 执行如下指令，判断网络是否可以连接。 PING IP 其中 IP 为其他CMS所在的节点IP地址。 如果提示连接超时，表明节点连接不上，需要联系网络管理员检查节点是否断网、掉电、虚拟机故障等。如果其他所有CMS所在节点全部断网、掉电、虚拟机故障，那么需要联系环境管理员查看环境是否正常。环境修复完成后需要查看是否有CN节点被剔除，如果有CN节点被剔除，需要参考 节点修复 修复CN节点。修复完成后，如果CMS组件都已恢复正常，则不需要执行后边的操作。 如果可连接CMS节点，执行如下指令，查看cm_server进程是否存在。 ps ux | grep cm_server 如果进程存在，则执行如下指令, 重启进程。并且重启成功后不需要再执行该章节内容。 kill -9 cms进程PID 如果进程不存在，执行下列步骤排除CMS目录是否正常，所在磁盘是否满，端口是否被占用。 执行如下指令，查看CMS所在目录。 cm_ctl view | grep cmDataPath 图3 CMS所在目录 执行如下指令，查看CMS目录是否存在。 ls CMS所在目录 执行如下指令，查看目录中文件是否丢失或被更改。 ls CMS所在目录/cm_server 需要在部署CMS的节点上查看，没有部署CMS的节点上查询不到CMS目录。 图4 查找目录 如果发现CMS配置文件不存在或被删导致的故障，则参考 修复CMS配置文件 修复CMS。 执行如下指令查看磁盘占用率。 df CMS所在目录 /cm_server CMS所在目录为在 6 获取的路径。 图5 查看磁盘 查看回显关键词Use%是否已经为100%了，如果是，执行 exit 退出沙箱后，参考 清理磁盘 修复CMS。 执行如下指令确定CMS端口号。 cm_ctl view | grep -w 'port' 切换到root用户，执行如下指令，确认是否有其他进程占用该端口。 lsof -i: port | grep LISTEN port为 8 中查看到CMS端口号。 如果查询到CMS侦听端口被其他进程占用，切换到Ruby用户并进入沙箱，参考 释放CMS端口 修复CMS。 清理磁盘 如果所有CMS都磁盘满了，先选择一个CMS，清理磁盘 。 在CMS所在节点，执行如下指令，查看是否有文件句柄泄漏。 lsof | grep deleted 如果存在，执行如下指令删除泄漏句柄的进程, 实现对磁盘的释放。 kill -9 PID 删除CMS磁盘中的无用文件，或把文件转移到其他磁盘。 恢复其中一个CMS后，手动执行如下指令启动集群，实现业务恢复。 cm_ctl start 恢复业务后，重复 2 、 3 再清理其他CMS磁盘空间。 执行完成后请进行 恢复确认 。 修复CMS配置文件 当CMS配置文件被误删除时，执行如下指令修复文件。 cp  usr/local/core/app/share/config/cm_server.conf.sample cm_server数据目录 / cm_server.conf cm_server数据目录 为 6 查询到的故障目录。 执行如下命令修改cm_server.conf文件中的log_dir参数, 设置CMS的日志路径。 vim /var/lib/engine/data1/cm/cm_server/cm_server.conf 这里是在沙箱环境下，所以需要去掉目录路径前面的沙箱目录/var/chroot。 输入“i”进入编辑模式，在其中新增以下字段： log_dir = $GAUSSLOG/cm/cm_server 输入“：wq”，保存退出。 查看修改结果。 cat /var/chroot/var/lib/engine/data1/cm/cm_server/cm_server.conf 图6 设置路径 执行如下指令，启动集群。 cm_ctl start 按照 1 、 2 修复剩余CMS。 执行完成后请进行 恢复确认 。 释放CMS端口 执行如下指令，查看cms的侦听端口。 cm_ctl view | grep - w 'port' 图7 查看端口 执行如下指令，切换到root用户 exit 执行如下指令，查看是否有其他进程占用该端口 lsof -i: port | grep LISTEN port 为 1 中查到的端口号。 图8 占用端口号 如果发现占用该端口的不是cm_server，执行如下指令，删除占用端口的进程。 kill -9 PID 释放端口后，CMS会自动启动。 切换到Ruby用户，执行如下指令，启动集群。 su - Ruby source /home/Ruby/gauss_env_file cm_ctl start 重复 1 至 5 恢复其他故障CMS。 执行完成后请进行 恢复确认 。 恢复确认 登录到集群任一节点查询集群状态，如果所有CMS组件都已经恢复正常则说明修复成功，否则请继续排查原因，如果所有原因都已经排查完成，CMS节点仍然异常，请联系技术人员处理。 父主题： 租户面应急案例 版权所有 © 华为技术有限公司
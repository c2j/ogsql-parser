<!-- description: JDBC {? = call} WITHOUT statementType=CALLABLE — wrapper NOT stripped -->
<!-- statements: 1 -->
<!-- namespace: com.example.ProcMapper -->
<!-- flat-contains: {? = call fnc_com_getday -->
<!-- params: scdm, day, feed, coinCode -->
<!-- has-dynamic: false -->
<mapper namespace="com.example.ProcMapper">
    <select id="callGetDay" resultType="map" parameterType="map">
        {? = call fnc_com_getday(
        #{scdm,mode=IN,jdbcType=VARCHAR},
        #{day,mode=IN,jdbcType=VARCHAR},
        #{feed,mode=IN,jdbcType=INTEGER},
        #{coinCode,mode=IN,jdbcType=VARCHAR}
        )}
    </select>
</mapper>

<!-- description: Nested unclosed tag error silently swallowed (Bug 2) -->
<!-- statements: 1 -->
<!-- namespace: t -->
<!-- flat-contains: SELECT, __XML_PARAM_i__ -->
<!-- has-dynamic: true -->
<!-- params: i -->
<mapper namespace="t">
    <select id="q1">
        SELECT * FROM t WHERE
        <foreach collection="list" item="i" open="(" close=")" separator=",">
            #{i}
        </foreach
    </select>
</mapper>
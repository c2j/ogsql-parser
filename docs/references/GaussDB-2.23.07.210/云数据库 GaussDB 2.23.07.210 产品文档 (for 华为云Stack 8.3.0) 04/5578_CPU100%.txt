CPU飙升100%应急处理 CPU飙升100%应急处理 故障现象 CPU持续100%，收到大量CPU告警。 故障影响 数据库无法响应业务请求或响应时间出现异常。 处理思路 CPU使用率达到100%，首先需确认业务运行情况： 如业务已经阻塞或运行缓慢不可接受，需先执行 快速恢复 步骤 。 如业务运行缓慢，但是处于可接受状态，则先执行 故障定位处理 步骤 。 快速恢复 登录DBS运维管理平台 。 进入“监控运维 > 实例监控 > 实例监控”页面，以实例ID搜索。 查看CPU使用率指标，选择全部节点，按节点展示，确认CPU100%的节点。 参考 登录实例节点 ，登录CPU100%的节点。 查询集群状态，确认各组件状态是否正常。 cm_ctl query -Cvid 若组件正常，继续 6 。 若组件状态异常，则需要联系华为技术支持。 查看各进程CPU使用情况，查看gaussdb进程CPU使用率。 top -c 确认是gaussdb进程CPU使用率高，记录此pid，一般CPU使用率高的进程均为主DN进程，以下统称主DN进程。 若节点执行指令没有明显卡顿，则执行 7 。 确认是gaussdb进程CPU使用率高，且节点执行指令卡顿或超过2~5min未返回数据，则直接执行 15 。 按q退出top命令。 执行如下命令，查看并记录主DN进程资源使用明细。 top -Hp pid pid从 6 获取。 按q退出top命令。 执行如下命令，查看并记录内存使用情况。 free -m 参考 登录实例节点 登录节点，使用gsql连接系统库。 gsql -d postgres -p 8000 -U user -r user需替换为实际的系统管理员账号，此处需要输入密码。 分布式登录CN节点，如果CPU100%的节点上有CN组件，则应连接CPU100%的节点，如果CPU100%的节点不存在CN组件，则登录其他任意正常的CN节点。 主备版登录主DN节点。 查询并记录数据库等待事件。 分布式： select wait_event,count(*) from pgxc _thread_wait_status group by 1 order by 2 desc; 主备版： select wait_event,count(*) from pg_thread_wait_status group by 1 order by 2 desc; 等待事件可参考《开发指南》 pg_thread_wait_status 视图说明。 可在实例管理详情页面->诊断优化->会话管理的实时会话中快速查看到等待事件信息，并支持按照等待事件排序展示会话列表。 执行如下命令，查看SQL执行情况。 每10s执行一次，记录回显，共执行3次，确定执行时间长的TOP SQL以及涉及的database及user，并记录。 分布式： execute direct on all 'select current_timestamp - query_start as runtime, datname, usename, pid, sessionid, substr(query,0,100) from pg_stat_activity where state != ''idle'' and datname !=''postgres'' order by runtime desc;'; 主备版： select current_timestamp - query_start as runtime, datname, usename, pid, sessionid, substr(query,0,100) from pg_stat_activity where state != 'idle' and datname !='postgres' order by runtime desc; 如上图，该SQL执行时间为2min10s，SQL所在的数据库为db_test，用户为Ruby。 可在实例管理详情页面->诊断优化->SQL视图的TOP SQL列表，通过对SQL的CPU、I/O进行排序，快速找到资源消耗最大的SQL。 执行如下命令，停止执行时间超过1min（时间可根据业务自定义）SQL语句。 此步骤可执行2~5次，同时查询CPU使用率是否降低。 停止正在执行的SQL语句为高危操作，执行前需联系客户及华为技术确认。 分布式： execute direct on all 'select pg_terminate_session(pid,sessionid),pid,sessionid from pg_stat_activity where state != ''idle'' and datname !=''postgres'' and datname ='' database '' and usename = '' user' ' and extract(epoch from current_timestamp-xact_start)/60 > 1;'; 主备版： select pg_terminate_session(pid,sessionid),pid,sessionid from pg_stat_activity where state != 'idle' and datname !='postgres' and datname =' database ' and usename =' user ' and extract(epoch from current_timestamp-xact_start)/60 > 1; pid列返回t表示执行成功。 如上图，停止执行时间大于1min，数据库为db_test，用户为Ruby执行的SQL语句。 可在实例管理详情页面->诊断优化->会话管理的实时会话列表，选取执行超过一分钟的会话，一键查杀会话。支持批量查杀会话信息。 database，user从 11 获取到， and datname =' database ' and usename =' user ' 查询条件可根据实际情况更改。 extract(epoch from current_timestamp-xact_start)/60 > 1中”1”表示停止执行超过1min的SQL，可根据实际情况修改。 以上为批量停止会话方法，如果需要停止单个会话，执行语句为： 分布式：execute direct on all 'select pg_terminate_session(pid,sessionid) ;'; 主备版： select pg_terminate_session(pid,sessionid) ; pid和sessionid由 11 获取。 停止会话语句后，执行如下命令，退出gsql连接。 \q 执行如下命令，查看CPU使用率。 top -c 若CPU使用率降低或恢复至正常水平(对比历史CPU监控)，则持续观察业务，确认业务稳定后，执行 信息收集 步骤。 若停止会话语句后，CPU使用率未明显降低，或者降低后又升高至100%，则继续 15 。 按q退出top命令。 杀掉主DN进程。 kill -9 pid pid替换为 6 获取的主DN的pid。 自动回车至下一行表示执行成功。 可在DBS运维管理平台->实例管理实例详情页，停止对应DN进程。 kill进程为高危操作，执行前需联系客户及华为技术确认。 查询集群状态，确认主DN切换到其他节点，并获取记录。 cm_ctl query -Cvid 停止主DN进程后，实例会进行主备切换。此时，需持续观察集群CPU使用情况： 若新主DN所在节点CPU使用率处于正常水平(对比历史CPU监控)，执行 信息收集 步骤。 若新主DN所在节点CPU使用率再次升高至100%，则需联系客户停止应用程序，待集群CPU使用率降低到正常水平以后，重新启动应用程序，确认业务恢复后持续观察，稳定后执行 信息收集 步骤。 确认业务彻底恢复，执行DN回切操作。 cm_ctl switchover -n nodeid -D dn数据目录 nodeid和dn数据目录可以在 5 获取。 例：将6001回切 cm_ctl switchover -n 1 -D /data/gaussdb/engine/data/dn_6001 回切步骤需确认业务恢复，定位到根因且解决之后执行。 故障定位处理 执行如下步骤，定位故障。 登录DBS运维管理平台 ，进入“监控运维 > 实例监控 > 实例监控”。 输入对应实例ID，选择全部节点，查询实例监控。 查看一周的95%SQL的响应时间、80%SQL的响应时间、活跃会话数量、用户DML语句数监控。 若近期(2~7天)活跃会话数量及用户DML语句数持续处于高位，表示业务负载增加，需要评估业务负载，并参考规格变更进行扩容或者业务限流等措施。 若短期内活跃会话数量及用户DML语句数增加，同时95%SQL的响应时间、80%SQL的响应时间升高，则可能存在慢SQL，或存在锁冲突等，继续执行 2 。 参考 登录实例节点 ，登录CPU100%的节点，一般为主DN节点。 执行以下命令，查看各进程CPU使用情况。 top -c 确认是gaussdb进程CPU使用率高，记录此pid，一般CPU使用率高的进程均为主DN进程，以下统称主DN进程。若其他进程高，占用CPU，则联系华为技术支持。 按q退出命令。 执行如下命令，查看并记录主DN进程资源使用明细。 top -Hp pid pid可以在 3 获取。 按q退出命令。 参考 登录实例节点 登录节点，使用gsql连接系统库。 gsql -d postgres -p 8000 -U user -r user请替换为实际的系统管理员账号，此处需要输入密码。 分布式登录CN节点，如果CPU100%的节点上有CN组件，则应连接CPU100%的节点，如果CPU100%的节点不存在CN组件，则登录其他任意正常的CN节点。 主备版登录主DN节点。 执行如下命令，排查是否存在长时间持锁的SQL。 分布式： execute direct on all 'select substr(w.query,0,100),l.granted,w.pid,w.datname,w.usename from pg_stat_activity w join pg_locks l on w.pid = l.pid where state != ''idle'' and w.datname !=''postgres'' and extract(epoch from current_timestamp-w.xact_start)/60 > 1 and l.granted=true;'; 主备版： select substr(w.query,0,100),l.granted,w.pid,w.datname,w.usename from pg_stat_activity w join pg_locks l on w.pid = l.pid where state != 'idle' and w.datname !='postgres' and extract(epoch from current_timestamp-w.xact_start)/60 > 1 and l.granted=true; 如上图，回显有数据表示存在持锁超过1min（时间可根据实际情况确定）的语句，执行库为db_test，用户为Ruby。 若存在锁，则表示当前SQL语句为长时间锁持有者，通常会阻塞业务，可获取databse及user并执行 7 结束该锁，同时排查业务逻辑是否合理。 若不存在锁，继续 8 。 extract(epoch from current_timestamp-xact_start)/60 > 1中”1”表示持锁超过1min的SQL，可根据实际情况修改。 执行如下命令，停止长时间持锁的SQL语句。 停止正在执行的SQL语句为高危操作，执行前需联系客户及华为技术确认。 分布式： execute direct on all 'select pg_terminate_session(w.pid,w.sessionid),w.pid,w.sessionid from pg_stat_activity w join pg_locks l on w.pid = l.pid where state != ''idle'' and w.datname !=''postgres'' and w.datname ='' database '' and w.usename ='' user '' and extract(epoch from current_timestamp-w.xact_start)/60 > 1 and l.granted=true;'; 主备版： select pg_terminate_session(w.pid,w.sessionid),w.pid,w.sessionid from pg_stat_activity w join pg_locks l on w.pid = l.pid where state != 'idle' and w.datname !='postgres' and w.datname = ' database ' and w.usename = ' user ' and extract(epoch from current_timestamp-w.xact_start)/60 > 1 and l.granted=true; pid返回t表示执行成功。 database，user从 6 中获取， and w.datname = ' database ' and w.usename = ' user '可根据实际情况更改，生产环境建议加上此条件。 extract(epoch from current_timestamp-xact_start)/60 > 1中”1”表示持锁超过1min的SQL，可根据实际情况修改。 以上为批量停止会话方法，如果需要停止单个会话，执行语句为： 分布式：execute direct on all 'select pg_terminate_session(pid,sessionid) ;'; 主备版： select pg_terminate_session(pid,sessionid) ; pid和sessionid由 6 获取。 执行如下命令，查询执行时间超过1min（时间可自定义）的SQL语句详细信息。 分布式： execute direct on all 'select current_timestamp - query_start as runtime,datname,usename,sessionid,substr(query,0,100) from pg_stat_activity where state != ''idle'' and datname !=''postgres'' and extract(epoch from current_timestamp-xact_start)/60 > 1 order by 1 desc;'; 主备版： select current_timestamp - query_start as runtime,datname,usename,sessionid,substr(query,0,100) from pg_stat_activity where state != 'idle' and datname !='postgres' and extract(epoch from current_timestamp-xact_start)/60 > 1 order by 1 desc; 如图，查询获取到执行时间超过1min（时间可自定义）的SQL语句。 extract(epoch from current_timestamp-xact_start)/60 > 1中”1”表示查询超过1min的SQL，可根据实际情况修改。 查询系统记录的慢SQL信息。 select node_name,substr(query,0,100) from dbe_perf.get_global_slow_sql_by_timestamp(' 2023-01-17 09:25:55 ',' 2023-01-18 19:25:55 '); 开始和结束时间根据实际情况修改。 须在系统库中查询，用户库无法查询。 通过 8 和 9 获取到的慢SQL信息，可获取TOP慢SQL以及涉及的表信息。 切换至用户库，执行以下SQL，查询 10 中涉及表的统计信息。 \c database select * from pg_stat_all_tables where relname in(' table_name 1 ',' table_name2 '); database为用户库。 table_name1，table_name2为查询的目标表名，多表可使用英文逗号隔开。 以上SQL执行结果主要关注n_dead_tup(死行数)，last_vacuum(最近一次vacuum时间)，last_autovacuum(最近一次自动vacuum时间)，last_analyze(最近一次analyze时间)，last_autoanalyze(最近一次自动analyze时间)的值，其他字段含义可详细参考《开发者指南》中 pg_stat_all_tables 系统视图说明。 如果某个database涉及多个表死行数比较多，则可执行 12 。 如果仅为少数表涉及统计信息未更新，则执行 13 。 如果涉及少数表死行较多，则执行 14 。 如上图，n_dead_tup=7，且统计信息未更新，则需要执行 14 。 在目标库下，执行以下命令，清理整库并且更新表统计信息。 vacuum full analyze ; vacuum full会对表加排他锁，且会导致I/O流量的大幅增加，可能会影响业务，执行需谨慎。 需在目标库下执行。 执行以下命令，更新统计信息。 analyze tablename ; tablename为查询的目标表名。 analyze需要在用户库下执行。 analyze会导致I/O流量的大幅增加，这可能会影响其他活动会话的性能，执行前需与客户确认。 执行以下命令，回收空间且更新统计信息。 vacuum analyze tablename ; tablename为查询的目标表名。 vacuum需要在用户库下执行。 vacuum会导致I/O流量的大幅增加，这可能会影响其他活动会话的性能，执行前需与客户确认。 执行之后的效果，可继续查询pg_stat_all_tables验证，如下图。 若通过以上步骤优化之后，仍然存在慢SQL，则对 8 和 9 获取到的慢SQL进行优化。 执行以上步骤之后，查看性能是否有所改善。 若有所改善则可继续优化慢SQL。 若无改善或者改善不显著，则在DBS运维管理平台>监控运维>实例监控>信息采集页签获取对应时间点的WDR报告，并联系华为技术支持。 信息收集 参考 下载租户节点日志 ，下载主备节点的 GaussDB Kernel日志，路径如下。 /var/lib/log/Ruby 参考 下载租户节点日志 ，下载ops日志，路径如下。 /home/Ruby/log/ops_collector.log 参考 登录实例节点 ，登录原主节点。 收集原主节点messages日志，sysmonitor.log日志。 messages、sysmonitor.log日志均在/var/log目录下。 收集原主节点dmesg 日志，获取方式如下： 按ctl + D退出至root用户，并执行如下命令，收集日志。 dmesg -T > /home/Ruby/ dmesg.log MCS若无sysmonitor日志，dmesg -T日志，则需要在当前节点所在物理机收集生成的dmesg.log文件。 参考 登录实例节点 ，gsql连接登录数据库，收集对应时间点30分钟内的gs_asp数据，执行如下命令收集日志： su - Ruby source /home/Ruby/gauss_env_file gsql -d postgres -p 8000 -U user -r user请替换为实际的系统管理员账号，此处需要输入密码。 分布式登录CN节点。 主备版登录主DN节点。 \o /home/Ruby/gsasp_tmp.log select * from gs_asp where sample_time > ' yyyy-mm-dd hh:mm ' and sample_time < ' yyyy-mm-dd hh:(mm+30min )'; 以2023年2月14日10:22为例： 执行后在/home/Ruby/路径下，会生成gsasp_tmp.log文件。 收集PG_STAT_ALL_TABLES表的全量数据，执行以下命令收集。 \o /home/Ruby/psat_tmp.log select * from pg_stat_all_tables; 执行“\q” 退出数据库。 执行后在/home/Ruby/目录下，会生成psat_tmp.log文件。 收集到以上所有步骤中的日志后，联系华为技术支持进一步分析。 恢复确认 使用gsql连接数据库，查看SQL执行情况，持续查询，确认业务SQL语句正常执行即可。 select pid,sessionid, substring(query,0,100), state,usename,now()-query_start as elp from pg_stat_activity where state != 'idle' order by elp desc; 父主题： 资源类故障应急案例 版权所有 © 华为技术有限公司
SQL分析 SQL分析 通过本章节可以查看对应数据库的表对象TOP SQL，慢SQL和慢SQL详情。 操作步骤 登录DBS运维管理平台 。 单击 “实例管理” 的 “实例列表” 页签。 选择对应的实例，单击实例ID，跳转到“DBA智能运维”页面。 单击“SQL分析”。 单击查看TOP SQL，慢SQL和慢SQL详情信息。 表1 TOP SQL信息说明 列名 说明 SQL ID 归一化的SQL ID。 用户名 用户名称。 SQL文本 归一化的SQL。 调用频率占比（%） 调用频率占比。 CPU开销占比（%） CPU开销占比。 IO开销占比（%） IO开销占比。 调用次数（次） 调用次数。 返回元组数（行） SELECT返回的结果集行数。 读取元组数（行） 扫描返回的结果行数，包含随机，顺序扫描行。 平均时间开销（us） SQL在内核内的平均运行时间（单位：微秒）。 总时间开销（us） SQL在内核内的总运行时间（单位：微秒）。 CPU开销（us） CPU开销时间（单位：微秒）。 IO开销（us） IO上花费的时间（单位：微秒）. 最小执行时间（us） SQL在内核内的最小运行时间（单位：微秒）。 最大执行时间（us） SQL在内核内的最大运行时间（单位：微秒） 表2 慢SQL信息说明 列名 说明 SQL ID 归一化的SQL ID。 用户名 用户名称。 SQL文本 归一化的SQL。 SQL计划 语句执行计划。 执行次数（次） 调用次数。 平均执行时间（us） 有效的DB平均花费时间，多线程时间累加（单位：微秒）。 平均CPU耗时（us） CPU平均花费时间（单位：微秒）。 平均IO耗时（us） IO上的平均花费时间（单位：微秒）。 平均返回行数（行） 顺序扫描平均行数。 平均扫描行数（行） 随机扫描平均行数。 buffer命中率 Buffer的块平均命中次数。 表3 慢SQL详情信息说明 列名 说明 SQL ID 归一化的SQL ID。 用户名 用户名称。 客户端IP 用户发起的请求的客户端地址。 客户端端口 用户发起的请求的客户端端口。 SQL文本 归一化SQL。 SQL计划 语句执行计划。 开始时间 语句启动的时间。 结束时间 语句结束的时间。 返回行数 SELECT返回的结果集行数。 扫描行数 随机扫描行。 扫描页面数 buffer的块访问次数。 总耗时 有效的DB花费时间，多线程将累加（单位：微秒）。 CPU耗时 CPU时间（单位：微秒）。 生成计划耗时 SQL生成计划时间（单位：微秒）。 IO耗时 IO上花费的时间（单位：微秒）。 等锁数量 加锁次数。 等锁耗时 加锁耗时。 父主题： DBA智能运维 版权所有 © 华为技术有限公司